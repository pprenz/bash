GIT

local
git branch --get branch 
git status
git checkout #branch_name --to change to branch
git pull

git push origin #branch_name --push/create new branch to unffudle.f

++++++++++++++++++++++++++++++
2:26 PM 4/20/2011
Portal Branch Git Push.

1. Mondays we push Git/portal 1 (it trigger portal 2)
2. Wendsday we push Git/portal 3(it triggers portal 4)

+++++++++++++++++++++++++++++

scheduler backtrace configuration in Jenkin job

-g this option is to specify one strategy out of possible : single, double, back

-x this option is to specify a max number of live lessons to schedule when the strategy is “back” or “double”

--------------------
specify one strategy out of possible
single
double
back
